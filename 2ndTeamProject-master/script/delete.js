const deleteComment = document.getElementsByID("comment_list")
const CMTLST = deleteComment.querySelector("input")

function DLCMT (event) {
const 

}
deleteComment.addEventListener("submit", deleteSubmit)